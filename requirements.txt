# Веб-интерфейс
streamlit>=1.30.0 # — веб‑интерфейс

# LangChain (ядро и интеграции)
langchain>=0.3.0
langchain-core>=0.3.0 # — основа для цепочек
langchain-community>=0.3.0 # — загрузчики документов (TextLoader, PyPDFLoader, DirectoryLoader)
langchain-text-splitters>=0.3.0 # — RecursiveCharacterTextSplitter
langchain-chroma>=0.1.2 # — подключение к векторной базе
langchain-huggingface>=0.1.0 # — локальные эмбеддинги через HuggingFaceEmbeddings
langchain-gigachat>=0.3.8 # — LLM от Сбера

# GigaChat API клиент
gigachat>=0.1.0 # — LLM от Сбера

# Векторная база данных
chromadb>=0.4.0 # — векторное хранилище

# Локальные эмбеддинги
sentence-transformers>=2.2.0 # — загрузка модели эмбеддингов (paraphrase-multilingual-MiniLM-L12-v2)

# Загрузка документов
pypdf>=4.0.0 # — чтение PDF
docx2txt>=0.8 # — чтение DOCX

# Переменные окружения
python-dotenv>=1.0.0 # — загрузка .env с ключами